# Аиша — Telegram-сервис для абитуриентов

## Описание проекта

“Аиша” — расширенный демонстрационный прототип Telegram-сервиса для абитуриентов на Python + aiogram 3. Бот помогает спокойно подобрать варианты вузов по региону, баллам ЕГЭ, направлению и типу обучения, разделяет варианты на безопасные, реалистичные и амбициозные, сохраняет избранное и позволяет сравнивать несколько программ.

В проекте также есть Telegram Mini App: простая веб-форма подбора вузов на HTML, CSS и JS. Интерфейс Mini App приведён к светлому стилю общего сайта команды “Аиша”: крупный hero-блок, карточка “Привет, я Аиша!”, тёмно-синие кнопки и светлые карточки результатов. OpenAI подключён опционально и используется только как дополнительный слой для мягких объяснений и поддержки.

Сейчас используются демонстрационные данные. Финальная база вузов будет подставлена перед сдачей, а логика проекта подготовлена к замене демонстрационного JSON на финальные данные без переписывания основного кода.

Старый C++ Telegram-модуль сохранён как архив в `archive/cpp_telegram_module/`.

## Возможности

- подбор вузов по региону, баллам, направлению и типу обучения;
- пошаговый сценарий через FSM;
- backend-заглушка `backend_stub` с HTTP endpoint `/api/universities`;
- расширенная демонстрационная база регионов и направлений;
- карточки результатов с категорией, предметами, баллами, формой, сроком, стоимостью и ссылкой;
- категории подбора: безопасные, реалистичные и амбициозные варианты;
- профиль пользователя и локальное хранение последнего подбора;
- избранные вузы и удаление сохранённых вариантов;
- сравнение вузов из последнего подбора или избранного;
- команда `/categories` с объяснением категорий;
- команда `/webapp` и кнопка Telegram Mini App при заданном `WEBAPP_URL`;
- светлый Mini App-интерфейс в стиле командного сайта “Аиша”;
- информационные команды `/about`, `/demo`, `/privacy`, `/next`, `/botfather`;
- психологическая поддержка с безопасными fallback-ответами;
- локальный safety-фильтр для кризисных сообщений;
- OpenAI wrapper через официальный SDK `openai` и `AsyncOpenAI`;
- fallback без `OPENAI_API_KEY`;
- автоматические проверки через pytest и `scripts/check_project.sh`.

## Архитектура

Основной поток подбора:

```text
Пользователь -> Telegram Bot -> backend_stub/API -> JSON-данные -> Telegram Bot -> Пользователь
```

Mini App использует тот же endpoint:

```text
Mini App -> backend_stub/API -> JSON-данные -> Mini App
```

OpenAI используется отдельно:

```text
Telegram Bot -> OpenAI API -> мягкое объяснение/поддержка -> Telegram Bot
```

Safety-фильтр стоит до OpenAI: сообщения про самоповреждение, суицидальные мысли или опасность для себя обрабатываются локально и не отправляются в OpenAI.

## Структура проекта

```text
telegram_bot/
  main.py                  # запуск Telegram-бота
  config.py                # настройки из .env
  handlers/                # команды, меню, поиск, поддержка, сравнение
  keyboards/               # клавиатуры меню, поиска и поддержки
  services/                # backend API, OpenAI, validation, safety, recommendation, texts
  states/                  # FSM состояния
  storage/                 # локальное JSON-хранилище

backend_stub/
  main.py                  # временный backend API и раздача Mini App
  data/universities.json   # демонстрационные данные вузов

mini_app/
  index.html               # Telegram Mini App страница
  styles.css               # стили Mini App
  app.js                   # логика формы и карточек
  favicon.svg              # favicon Mini App

archive/
  cpp_telegram_module/     # старый C++ Telegram-модуль

tests/                     # pytest-проверки
scripts/check_project.sh   # локальная проверка проекта
```

## Временная демонстрационная база вузов

Сейчас используется демонстрационная база `backend_stub/data/universities.json`. Она нужна для показа логики поиска, категорий, избранного, сравнения и Mini App.

База содержит несколько регионов и направлений:

- регионы: Адыгея, Москва, Краснодарский край, Санкт-Петербург, Татарстан, Крым, Ростовская область, Свердловская область;
- направления: IT, психология, медицина, экономика, юриспруденция, педагогика, дизайн, строительство, туризм.

Проходные баллы, стоимость и ссылки являются демонстрационными. Это не официальные данные и не гарантия поступления.

Ожидаемая структура записи:

```json
{
  "university": "Название вуза",
  "city": "Город",
  "region": "Регион",
  "program": "Программа",
  "direction": "Направление",
  "subjects": ["предмет 1", "предмет 2"],
  "min_score": 200,
  "type": "бюджет",
  "price": null,
  "url": "https://example.ru",
  "study_form": "очная",
  "duration": "4 года",
  "note": "демонстрационные данные"
}
```

Поля `study_form`, `duration`, `note`, `price` и `url` могут отсутствовать или быть пустыми. Код должен показывать аккуратные fallback-значения, а не технические `None` или `null`.

## Как заменить данные перед сдачей

Перед сдачей можно заменить временный файл `backend_stub/data/universities.json` на финальную базу вузов, сохранив структуру полей. Основная логика подбора, категорий, сравнения, избранного и Mini App уже работает через этот JSON и не требует переписывания.

После замены файла нужно выполнить:

```bash
bash scripts/check_project.sh
```

И вручную проверить несколько сценариев подбора в Telegram и Mini App.

## Установка

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Настройка `.env`

Создайте локальный `.env`:

```bash
cp .env.example .env
```

Пример:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
BACKEND_BASE_URL=http://localhost:8000
WEBAPP_URL=
```

`TELEGRAM_BOT_TOKEN` обязателен для запуска Telegram-бота.

`OPENAI_API_KEY` можно оставить пустым. Тогда бот работает через fallback.

`WEBAPP_URL` можно оставить пустым. Тогда кнопка Mini App не появляется в меню, команда `/webapp` показывает подсказку, а обычный бот работает как раньше.

Настоящий `.env` не должен попадать в Git.

## Запуск backend

В первом терминале:

```bash
source .venv/bin/activate
python -m backend_stub.main
```

Проверка API:

```bash
curl "http://localhost:8000/api/universities?region=Адыгея&score=230&direction=IT&type=budget"
```

## Запуск Telegram-бота

Во втором терминале:

```bash
source .venv/bin/activate
python -m telegram_bot.main
```

## Telegram Mini App

Mini App находится в папке `mini_app/` и остаётся простым: HTML + CSS + JS без React, Vite, Next.js и других frontend-фреймворков.

Визуально Mini App приведён к стилю общего сайта команды “Аиша”: светлый серо-бежевый фон, тёмно-синий основной цвет, крупный заголовок “Построй будущее уже сегодня”, карточка “Привет, я Аиша!”, светлая форма подбора и аккуратные карточки результатов.

Локальная проверка:

```bash
source .venv/bin/activate
python -m backend_stub.main
```

Затем открыть в браузере:

```text
http://localhost:8000/miniapp
```

Mini App обращается к:

```text
/api/universities?region=...&score=...&direction=...&type=...&limit=5
```

OpenAI и токены в Mini App не используются.

## Проверка через ngrok

Для открытия Mini App внутри Telegram нужен публичный HTTPS URL. Локальный `http://localhost:8000/miniapp` подходит для проверки в браузере, но обычно не откроется на телефоне внутри Telegram.

Пример настройки через публичный HTTPS-туннель:

```env
WEBAPP_URL=https://your-public-url/miniapp
```

После изменения `WEBAPP_URL` нужно перезапустить Telegram-бота. Ngrok/cloudflared не являются зависимостями проекта и не настраиваются автоматически.

## BotFather: Menu Button, Main App, Direct Link

Команда `/botfather` в боте показывает готовую подсказку.

Команды для `/setcommands`:

```text
start - запуск бота
menu - главное меню
search - подбор вузов
compare - сравнение вузов
categories - как читать категории
support - психологическая поддержка
webapp - открыть Mini App
about - о проекте
demo - сценарий демонстрации
privacy - приватность
next - что подготовить перед сдачей
reset - сброс введённых данных
help - помощь
```

Menu Button:

```text
URL = WEBAPP_URL из .env
Название = Открыть Аишу
```

Main App:

```text
URL = WEBAPP_URL из .env
Название = Аиша
```

Direct Link можно создать для быстрого запуска Mini App, но для демонстрации достаточно `/webapp` и Menu Button.

## OpenAI

OpenAI подключён через официальный Python SDK `openai`, используется `AsyncOpenAI`.

Ключ берётся только из локального `.env`:

```env
OPENAI_API_KEY=your_openai_api_key_here
```

Если ключ пустой, бот не делает запросы к OpenAI и работает через fallback. Если OpenAI временно недоступен, ошибка логируется в консоль, а пользователь получает обычный безопасный ответ.

Safety/crisis-сообщения не отправляются в OpenAI.

## Категории подбора

После поиска Аиша делит варианты на категории:

- 🟢 безопасный вариант — минимальный балл ниже твоего с заметным запасом;
- 🟡 реалистичный вариант — минимальный балл близко к твоему;
- 🔴 амбициозный вариант — немного не хватает, но вариант можно рассмотреть как цель.

Недоступные варианты, где минимальный балл выше твоего больше чем на 20 баллов, не показываются в основной выдаче.

Категории помогают спокойно разложить варианты, но не гарантируют поступление.

## Профиль, избранное и сравнение

Бот локально сохраняет:

- Telegram ID;
- регион;
- баллы;
- направление;
- тип обучения;
- последние результаты;
- избранные вузы.

Пользователь может открыть “Мой профиль”, сохранить варианты в “Избранные вузы”, удалить отдельный сохранённый вуз или очистить избранное.

Сравнение доступно через `/compare` или кнопку “Сравнить вузы”. Сравнивать можно последние результаты подбора или избранные вузы. Бот показывает более спокойный и более амбициозный вариант, запас или нехватку баллов, стоимость, предметы, форму и срок обучения, если данные указаны.

Локальный файл `telegram_bot/storage/user_data.json` добавлен в `.gitignore` и не должен попадать в Git.

## Тесты

Быстрая проверка:

```bash
python3 -m compileall telegram_bot backend_stub tests
python3 -m json.tool backend_stub/data/universities.json > /tmp/universities_check.json
pytest
```

Через скрипт:

```bash
chmod +x scripts/check_project.sh
bash scripts/check_project.sh
```

Скрипт не запускает Telegram-бота и не требует реальных токенов.

## Команды

Основные:

- `/start` — запуск бота;
- `/menu` — главное меню;
- `/search` — подбор вузов;
- `/compare` — сравнение вузов;
- `/categories` — как читать категории;
- `/support` — психологическая поддержка;
- `/webapp` — открыть Mini App.

Информация:

- `/about` — о проекте;
- `/demo` — сценарий демонстрации;
- `/privacy` — приватность;
- `/next` — что подготовить перед сдачей;
- `/botfather` — настройки BotFather;
- `/help` — помощь.

Сервис:

- `/reset` — сброс профиля, последнего подбора и избранного.

## Демо-сценарий

Короткий сценарий:

1. `/start`
2. `Подобрать вуз`
3. `Адыгея`
4. `230`
5. `айти`
6. `Бюджет`
7. Проверить категории вариантов.
8. Сохранить 1–2 вуза.
9. Открыть “Избранные вузы”.
10. Открыть `/compare`.
11. Открыть `/webapp`.
12. Проверить `/support`.
13. Проверить safety-фразу: `не хочу жить`.

Полный сценарий демонстрации описан в [DEMO.md](DEMO.md).

## Ограничения демонстрационных данных

Данные в `backend_stub/data/universities.json` демонстрационные. Они показывают работу логики, но не являются официальными проходными баллами и не обещают поступление.

Финальная база вузов будет подставлена перед сдачей. Для этого нужно заменить JSON-файл, сохранив структуру полей, и повторить автоматические и ручные проверки.

## Статус проекта

Текущее состояние, ограничения и дальнейшие шаги описаны в [PROJECT_STATUS.md](PROJECT_STATUS.md).
