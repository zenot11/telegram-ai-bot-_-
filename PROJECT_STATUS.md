# Статус проекта “Аиша”

## Уже реализовано

- Telegram-бот на Python + aiogram 3;
- команды `/start`, `/menu`, `/help`, `/reset`, `/support`, `/search`, `/compare`, `/categories`;
- главное меню;
- FSM-подбор вузов;
- backend-заглушка;
- расширенные JSON-данные вузов для демонстрации;
- подробные карточки результатов с формой, сроком и пометкой;
- классификация вариантов по уровню риска: безопасные, реалистичные, амбициозные;
- хранение пользовательских данных локально;
- профиль пользователя;
- избранные вузы;
- сохранение вузов из последнего подбора;
- динамические кнопки сохранения показанных вузов;
- сравнение вузов;
- сравнение последних результатов;
- сравнение избранных вузов;
- вывод безопасного и амбициозного варианта;
- сравнение с учётом запаса или нехватки баллов пользователя;
- удаление конкретного избранного вуза;
- тесты для backend, данных, карточек, клавиатур, storage, safety, categories, compare и AI fallback;
- улучшенный профиль пользователя;
- психологическая поддержка;
- safety-фильтр;
- реальная OpenAI-интеграция через официальный SDK `openai` и `AsyncOpenAI`;
- fallback без OpenAI;
- README;
- DEMO.md;
- архив старого C++ Telegram-модуля.

## Что является демонстрационным

- `backend_stub`;
- `universities.json`;
- проходные баллы;
- стоимость, форма обучения, срок и ссылки;
- категории риска поступления;
- ссылки на сайты вузов;
- список направлений и регионов;
- локальное JSON-хранилище пользователя.

## Что можно улучшить дальше

- подключить настоящий backend API;
- заменить JSON на базу данных;
- добавить PostgreSQL;
- добавить админ-панель;
- удаление и сравнение большего количества вузов;
- расширить OpenAI-сценарии;
- добавить тесты handlers;
- добавить деплой;
- добавить Mini App позже;
- добавить личный кабинет абитуриента;
- сравнение по общежитию;
- сравнение по военной кафедре;
- сравнение по отзывам;
- сравнение по расстоянию до города пользователя;
- визуальную таблицу сравнения в Mini App позже;
- более крупная база вузов.

## Риски и ограничения

- данные вузов неофициальные;
- `backend_stub` нужен только для MVP;
- OpenAI работает только при наличии `OPENAI_API_KEY`;
- при отсутствии OpenAI используется fallback;
- `storage/user_data.json` локальный и не предназначен для продакшена;
- избранное хранится локально в JSON и подходит только для MVP;
- категории подбора являются ориентиром для MVP и не гарантируют поступление;
- расширенная база нужна для защиты и демонстрации, но всё ещё не является официальной базой вузов;
- проект пока не содержит настоящей базы вузов.

## Текущая архитектура

Основной поток:

```text
Пользователь → Telegram Bot → backend_stub/API → JSON-данные → Telegram Bot → Пользователь
```

AI-слой:

```text
Telegram Bot → OpenAI API → AI-объяснение/поддержка
```

Safety-фильтр стоит до OpenAI: сообщения про самоповреждение, суицидальные мысли или опасность для себя обрабатываются локально и не отправляются в OpenAI.
